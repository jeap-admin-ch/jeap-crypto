# Architecture

jEAP Crypto provides **client-side** encryption for data at-rest. The application encrypts and
decrypts data itself; a Key Management Service (KMS) is used only to generate and unwrap short-lived
data keys. This is independent of the persistence technology, in contrast to server-side encryption.

## What is encryption-at-rest?

Encryption-at-rest protects persisted data — database rows, S3 objects, Kafka messages — so that
an attacker who gains access to the storage medium cannot read the data without also possessing the
encryption keys. It reduces the attack surface against lower layers of the hardware and software
stack, and centrally managed wrapping keys create a single, auditable chokepoint for data access.

jEAP Crypto provides encryption-at-rest only. It does **not** encrypt data in transit (use TLS or
the jEAP JWE / JSON Web Encryption Starter for that) and provides no access protection for data
held in application memory.

## Core principles

- Symmetric encryption of data with **AES-256-GCM**.
- Encryption/decryption happen in the library (client-side).
- Short-lived **data keys** are used for the data; the **wrapping key** never leaves the KMS.
- A defined binary **container format** stores the encrypted data together with the wrapped data key
  and the required metadata.

## System overview

```mermaid
flowchart LR
    subgraph ENC["Encryption"]
        APP1["App"]
        CRYPTO1["jeap-crypto
Library"]
        APP1 -->|"1 Encrypt data"| CRYPTO1
    end

    subgraph KMS_AREA["Key Management"]
        KMS["Key Management
Vault / AWS KMS"]
        WK["Wrapping Key"]
        WK --- KMS
    end

    DB[("Data
at-rest")]

    subgraph DEC["Decryption"]
        APP2["App"]
        CRYPTO2["jeap-crypto
Library"]
        APP2 -->|"4 Decrypt data"| CRYPTO2
    end

    CRYPTO1 -->|"2 Request data key"| KMS
    KMS -->|"2 Encrypted data key
+ plaintext data key"| CRYPTO1

    CRYPTO1 -->|"3 Encrypted data key
+ ciphertext"| DB

    DB -->|"4 Encrypted data key
+ ciphertext"| CRYPTO2

    CRYPTO2 -->|"5 Unwrap encrypted
data key"| KMS
    KMS -->|"6 Decrypted data key"| CRYPTO2

    CRYPTO2 -->|"7 Plaintext data"| APP2

    classDef app fill:#cfe2f3,stroke:#333,stroke-width:1px
    classDef crypto fill:#d9edf7,stroke:#333,stroke-width:1px
    classDef kms fill:#eeeeee,stroke:#333,stroke-width:1px
    classDef key fill:#f4cccc,stroke:#333,stroke-width:1px
    classDef db fill:#d9ead3,stroke:#333,stroke-width:1px

    class APP1,APP2 app
    class CRYPTO1,CRYPTO2 crypto
    class KMS kms
    class WK key
    class DB db
```

## Encryption / decryption flow

```mermaid
sequenceDiagram
    participant App
    participant Crypto as jEAP Crypto
    participant KMS

    App->>Crypto: encrypt(plaintext)
    Crypto->>KMS: generate data key (with wrapping key)
    KMS-->>Crypto: data key (plaintext + wrapped)
    Crypto->>Crypto: AES-256-GCM encrypt with plaintext data key
    Crypto-->>App: container = wrapped data key + ciphertext + nonce

    App->>Crypto: decrypt(container)
    Crypto->>KMS: unwrap data key (with wrapping key)
    KMS-->>Crypto: plaintext data key
    Crypto->>Crypto: AES-256-GCM decrypt
    Crypto-->>App: plaintext
```

On encryption the library requests a data key from the KMS, which returns it both in plaintext (used
in memory to encrypt the data) and wrapped with the wrapping key. **Only the wrapped form is
persisted.** On decryption the wrapped data key from the container is sent to the KMS, which returns
the plaintext data key used to decrypt the data.

## Key concepts

| Term            | Meaning                                                                                           |
|-----------------|---------------------------------------------------------------------------------------------------|
| Data key        | Short-lived AES-256 key that encrypts the actual data. Generated by the KMS, may be cached.       |
| Wrapping key    | Long-lived key in the KMS that encrypts (wraps) data keys. Never leaves the KMS.                  |
| Crypto container| Self-describing binary format holding the wrapped data key, the ciphertext and metadata.          |
| Escrow key      | Optional extra (asymmetric) key that wraps the data key client-side for KMS-independent recovery. |

## Module overview

The public API lives in `jeap-crypto-core` (package `ch.admin.bit.jeap.crypto.api`). A KMS backend
(`jeap-crypto-vault` or `jeap-crypto-aws-kms`) implements the internal `KeyManagementService`.
`jeap-crypto-spring` adds the Spring Boot auto-configuration and registers a `CryptoService` bean per
configured wrapping key; the `jeap-crypto-vault-starter` / `jeap-crypto-aws-kms-starter` modules pull
everything together. `jeap-crypto-db` and `jeap-crypto-s3` are thin helpers on top of the APIs.

## Related

- [Getting started](getting-started.md)
- [Crypto APIs](crypto-api.md)
- [Key management](key-management.md)
- [Binary container format](data-format.md)
- [jeap-crypto](../README.md)
